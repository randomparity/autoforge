[runner]
phase = "all"
log_level = "info"
poll_interval = 30

[paths]
source_dir = "projects/vllm/repo"    # vLLM submodule; only needed for source builds
build_dir = "/tmp/vllm-build"

[timeouts]
build_minutes = 60
test_minutes = 20
